---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: wazuhfilebeats.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: WazuhFilebeat
    listKind: WazuhFilebeatList
    plural: wazuhfilebeats
    shortNames:
    - wfb
    singular: wazuhfilebeat
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.alerts.enabled
      name: Alerts
      type: boolean
    - jsonPath: .spec.archives.enabled
      name: Archives
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: WazuhFilebeat is the Schema for the wazuhfilebeats API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WazuhFilebeat
            properties:
              alerts:
                description: Alerts configures the alerts module
                properties:
                  enabled:
                    default: true
                    description: Enabled enables/disables alerts shipping
                    type: boolean
                type: object
              archives:
                description: Archives configures the archives module
                properties:
                  enabled:
                    default: false
                    description: Enabled enables/disables archives shipping
                    type: boolean
                type: object
              clusterRef:
                description: ClusterRef references the WazuhCluster this configuration
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              logging:
                description: Logging configures Filebeat logging
                properties:
                  keepFiles:
                    default: 7
                    description: KeepFiles is the number of log files to retain
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  level:
                    default: info
                    description: Level is the logging level
                    enum:
                    - debug
                    - info
                    - warning
                    - error
                    type: string
                  toFiles:
                    default: true
                    description: ToFiles enables logging to files
                    type: boolean
                type: object
              output:
                description: Output configures the OpenSearch output (optional override)
                properties:
                  credentialsSecretRef:
                    description: CredentialsSecretRef references a Secret containing
                      credentials
                    properties:
                      externalSecretRef:
                        description: |-
                          ExternalSecretRef references an ExternalSecret from ESO
                          When specified, the operator will read credentials from the K8s Secret
                          created by the ExternalSecret controller
                        properties:
                          name:
                            description: |-
                              Name of the ExternalSecret resource that will be created/referenced
                              The ExternalSecret will sync the secret from the external provider to a K8s Secret
                            type: string
                          namespace:
                            description: Namespace of the ExternalSecret (defaults
                              to same namespace as the parent resource)
                            type: string
                          refreshInterval:
                            default: 1h
                            description: RefreshInterval is how often to sync the
                              secret (e.g., "1h", "30m")
                            type: string
                          remoteRef:
                            description: RemoteRef specifies the remote secret location
                              in the external provider
                            properties:
                              key:
                                description: |-
                                  Key is the key/path in the external secret provider
                                  For Vault: "secret/data/myapp/config"
                                  For AWS SM: "myapp/database-credentials"
                                type: string
                              property:
                                description: |-
                                  Property is the specific property within the secret to extract
                                  If not specified, the entire secret is used
                                type: string
                              version:
                                description: Version is the version of the secret
                                  to fetch (provider-specific)
                                type: string
                            required:
                            - key
                            type: object
                          secretStoreRef:
                            description: SecretStoreRef references the SecretStore
                              or ClusterSecretStore to use
                            properties:
                              kind:
                                default: SecretStore
                                description: Kind is the kind of secret store (SecretStore
                                  or ClusterSecretStore)
                                enum:
                                - SecretStore
                                - ClusterSecretStore
                                type: string
                              name:
                                description: Name of the SecretStore or ClusterSecretStore
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - name
                        type: object
                      passwordKey:
                        default: password
                        description: PasswordKey is the key in the secret containing
                          the password
                        type: string
                      secretName:
                        description: SecretName references a native Kubernetes Secret
                        type: string
                      usernameKey:
                        default: username
                        description: UsernameKey is the key in the secret containing
                          the username
                        type: string
                    type: object
                  hosts:
                    description: Hosts is a list of OpenSearch hosts
                    items:
                      type: string
                    type: array
                  port:
                    default: 9200
                    description: Port is the OpenSearch port
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    default: https
                    description: Protocol is the protocol to use (http or https)
                    enum:
                    - http
                    - https
                    type: string
                type: object
              pipeline:
                description: Pipeline configures the ingest pipeline
                properties:
                  additionalRemoveFields:
                    description: AdditionalRemoveFields specifies additional fields
                      to remove
                    items:
                      type: string
                    type: array
                  customPipelineRef:
                    description: CustomPipelineRef references a ConfigMap containing
                      a custom pipeline
                    properties:
                      key:
                        description: Key is the key in the ConfigMap
                        type: string
                      name:
                        description: Name is the ConfigMap name
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoipEnabled:
                    default: true
                    description: GeoIPEnabled enables/disables GeoIP enrichment processors
                    type: boolean
                  indexPrefix:
                    default: wazuh-alerts-4.x
                    description: IndexPrefix is the prefix for index names
                    type: string
                  timestampFormat:
                    default: ISO8601
                    description: TimestampFormat is the format for parsing timestamps
                    type: string
                type: object
              ssl:
                description: SSL configures SSL/TLS settings
                properties:
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      CA certificate
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  clientCertSecretRef:
                    description: ClientCertSecretRef references a Secret containing
                      the client certificate
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  clientKeySecretRef:
                    description: ClientKeySecretRef references a Secret containing
                      the client key
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  verificationMode:
                    default: full
                    description: VerificationMode is the SSL verification mode
                    enum:
                    - full
                    - certificate
                    - none
                    type: string
                type: object
              template:
                description: Template configures the Wazuh index template
                properties:
                  additionalMappings:
                    description: AdditionalMappings allows adding custom field mappings
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  customTemplateRef:
                    description: CustomTemplateRef references a ConfigMap containing
                      a custom template
                    properties:
                      key:
                        description: Key is the key in the ConfigMap
                        type: string
                      name:
                        description: Name is the ConfigMap name
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  fieldLimit:
                    default: 10000
                    description: FieldLimit is the maximum number of fields per document
                    format: int32
                    maximum: 100000
                    minimum: 1000
                    type: integer
                  refreshInterval:
                    default: 5s
                    description: RefreshInterval is the index refresh interval
                    type: string
                  replicas:
                    default: 0
                    description: Replicas is the number of replica shards
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                  shards:
                    default: 3
                    description: Shards is the number of primary shards
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: status defines the observed state of WazuhFilebeat
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is the hash of the current configuration
                type: string
              configMapRef:
                description: ConfigMapRef references the generated ConfigMap
                properties:
                  key:
                    description: Key in the ConfigMap
                    type: string
                  name:
                    description: Name of the ConfigMap
                    type: string
                  namespace:
                    description: Namespace of the ConfigMap
                    type: string
                required:
                - name
                type: object
              lastAppliedTime:
                description: LastAppliedTime is when the config was last applied
                format: date-time
                type: string
              message:
                description: Message provides additional information
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase
                enum:
                - Pending
                - Ready
                - Failed
                - Updating
                type: string
              pipelineVersion:
                description: PipelineVersion is the version of the applied pipeline
                type: string
              templateVersion:
                description: TemplateVersion is the version of the applied template
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.alerts.enabled
      name: Alerts
      type: boolean
    - jsonPath: .spec.archives.enabled
      name: Archives
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WazuhFilebeat is the Schema for the wazuhfilebeats API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of WazuhFilebeat
            properties:
              alerts:
                description: Alerts configures the alerts module
                properties:
                  enabled:
                    default: true
                    description: Enabled enables/disables alerts shipping
                    type: boolean
                type: object
              archives:
                description: Archives configures the archives module
                properties:
                  enabled:
                    default: false
                    description: Enabled enables/disables archives shipping
                    type: boolean
                type: object
              clusterRef:
                description: ClusterRef references the WazuhCluster this configuration
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              logging:
                description: Logging configures Filebeat logging
                properties:
                  keepFiles:
                    default: 7
                    description: KeepFiles is the number of log files to retain
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                  level:
                    default: info
                    description: Level is the logging level
                    enum:
                    - debug
                    - info
                    - warning
                    - error
                    type: string
                  toFiles:
                    default: true
                    description: ToFiles enables logging to files
                    type: boolean
                type: object
              output:
                description: Output configures the OpenSearch output (optional override)
                properties:
                  credentialsSecretRef:
                    description: CredentialsSecretRef references a Secret containing
                      credentials
                    properties:
                      passwordKey:
                        default: password
                        description: Password key in secret
                        type: string
                      secretName:
                        description: Secret name
                        type: string
                      usernameKey:
                        default: username
                        description: Username key in secret
                        type: string
                    type: object
                  hosts:
                    description: Hosts is a list of OpenSearch hosts
                    items:
                      type: string
                    type: array
                  port:
                    default: 9200
                    description: Port is the OpenSearch port
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    default: https
                    description: Protocol is the protocol to use (http or https)
                    enum:
                    - http
                    - https
                    type: string
                type: object
              pipeline:
                description: Pipeline configures the ingest pipeline
                properties:
                  additionalRemoveFields:
                    description: AdditionalRemoveFields specifies additional fields
                      to remove
                    items:
                      type: string
                    type: array
                  customPipelineRef:
                    description: CustomPipelineRef references a ConfigMap containing
                      a custom pipeline
                    properties:
                      key:
                        description: Key is the key in the ConfigMap
                        type: string
                      name:
                        description: Name is the ConfigMap name
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  geoipEnabled:
                    default: true
                    description: GeoIPEnabled enables/disables GeoIP enrichment processors
                    type: boolean
                  indexPrefix:
                    default: wazuh-alerts-4.x
                    description: IndexPrefix is the prefix for index names
                    type: string
                  timestampFormat:
                    default: ISO8601
                    description: TimestampFormat is the format for parsing timestamps
                    type: string
                type: object
              ssl:
                description: SSL configures SSL/TLS settings
                properties:
                  caCertSecretRef:
                    description: CACertSecretRef references a Secret containing the
                      CA certificate
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  clientCertSecretRef:
                    description: ClientCertSecretRef references a Secret containing
                      the client certificate
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  clientKeySecretRef:
                    description: ClientKeySecretRef references a Secret containing
                      the client key
                    properties:
                      key:
                        default: password
                        description: Key is the key in the secret
                        type: string
                      name:
                        description: Name is the secret name
                        type: string
                    required:
                    - name
                    type: object
                  verificationMode:
                    default: full
                    description: VerificationMode is the SSL verification mode
                    enum:
                    - full
                    - certificate
                    - none
                    type: string
                type: object
              template:
                description: Template configures the Wazuh index template
                properties:
                  additionalMappings:
                    description: AdditionalMappings allows adding custom field mappings
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  customTemplateRef:
                    description: CustomTemplateRef references a ConfigMap containing
                      a custom template
                    properties:
                      key:
                        description: Key is the key in the ConfigMap
                        type: string
                      name:
                        description: Name is the ConfigMap name
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  fieldLimit:
                    default: 10000
                    description: FieldLimit is the maximum number of fields per document
                    format: int32
                    maximum: 100000
                    minimum: 1000
                    type: integer
                  refreshInterval:
                    default: 5s
                    description: RefreshInterval is the index refresh interval
                    type: string
                  replicas:
                    default: 0
                    description: Replicas is the number of replica shards
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                  shards:
                    default: 3
                    description: Shards is the number of primary shards
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: status defines the observed state of WazuhFilebeat
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is the hash of the current configuration
                type: string
              configMapRef:
                description: ConfigMapRef references the generated ConfigMap
                properties:
                  key:
                    description: Key in the ConfigMap
                    type: string
                  name:
                    description: Name of the ConfigMap
                    type: string
                  namespace:
                    description: Namespace of the ConfigMap
                    type: string
                required:
                - name
                type: object
              lastAppliedTime:
                description: LastAppliedTime is when the config was last applied
                format: date-time
                type: string
              message:
                description: Message provides additional information
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase
                enum:
                - Pending
                - Ready
                - Failed
                - Updating
                type: string
              pipelineVersion:
                description: PipelineVersion is the version of the applied pipeline
                type: string
              templateVersion:
                description: TemplateVersion is the version of the applied template
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
