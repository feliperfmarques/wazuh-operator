---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: opensearchsnapshots.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchSnapshot
    listKind: OpenSearchSnapshotList
    plural: opensearchsnapshots
    shortNames:
    - ossnapshot
    singular: opensearchsnapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.totalSize
      name: Size
      type: string
    - jsonPath: .status.duration
      name: Duration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchSnapshot is the Schema for triggering manual OpenSearch
          snapshots
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchSnapshotSpec defines the desired state of OpenSearchSnapshot
            properties:
              clusterRef:
                description: ClusterRef references the WazuhCluster this snapshot
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              ignoreUnavailable:
                default: true
                description: IgnoreUnavailable skips missing indices during snapshot
                type: boolean
              includeGlobalState:
                default: false
                description: IncludeGlobalState includes cluster state in the snapshot
                type: boolean
              indices:
                default:
                - wazuh-*
                description: Indices are index patterns to include in the snapshot
                items:
                  type: string
                type: array
              partial:
                default: false
                description: Partial allows partial snapshots (some shards may fail)
                type: boolean
              repository:
                description: |-
                  Repository is the name of the snapshot repository
                  The repository must exist (OpenSearchSnapshotRepository CRD)
                type: string
              waitForCompletion:
                default: true
                description: |-
                  WaitForCompletion blocks until snapshot completes
                  If false, snapshot is created asynchronously
                type: boolean
            required:
            - clusterRef
            - repository
            type: object
          status:
            description: OpenSearchSnapshotStatus defines the observed state of OpenSearchSnapshot
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duration:
                description: Duration is the snapshot duration (human-readable)
                type: string
              endTime:
                description: EndTime is when the snapshot completed
                format: date-time
                type: string
              indices:
                description: Indices are the indices included in the snapshot
                items:
                  type: string
                type: array
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              phase:
                description: Phase is the current phase (Pending, InProgress, Completed,
                  Failed, Partial)
                type: string
              shards:
                description: Shards contains shard statistics
                properties:
                  failed:
                    description: Failed shards
                    format: int32
                    type: integer
                  successful:
                    description: Successful shards
                    format: int32
                    type: integer
                  total:
                    description: Total number of shards
                    format: int32
                    type: integer
                required:
                - failed
                - successful
                - total
                type: object
              snapshotName:
                description: |-
                  SnapshotName is the generated snapshot name in OpenSearch
                  Format: {crd-name}-{yyyyMMdd}-{HHmmss}
                type: string
              startTime:
                description: StartTime is when the snapshot started
                format: date-time
                type: string
              state:
                description: State is the OpenSearch snapshot state (IN_PROGRESS,
                  SUCCESS, FAILED, PARTIAL)
                type: string
              totalSize:
                description: TotalSize is the total snapshot size (human-readable)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.totalSize
      name: Size
      type: string
    - jsonPath: .status.duration
      name: Duration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenSearchSnapshot is the Schema for triggering manual OpenSearch
          snapshots
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchSnapshotSpec defines the desired state of OpenSearchSnapshot
            properties:
              clusterRef:
                description: ClusterRef references the WazuhCluster this snapshot
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              ignoreUnavailable:
                default: true
                description: IgnoreUnavailable skips missing indices during snapshot
                type: boolean
              includeGlobalState:
                default: false
                description: IncludeGlobalState includes cluster state in the snapshot
                type: boolean
              indices:
                default:
                - wazuh-*
                description: Indices are index patterns to include in the snapshot
                items:
                  type: string
                type: array
              partial:
                default: false
                description: Partial allows partial snapshots (some shards may fail)
                type: boolean
              repository:
                description: |-
                  Repository is the name of the snapshot repository
                  The repository must exist (OpenSearchSnapshotRepository CRD)
                type: string
              waitForCompletion:
                default: true
                description: |-
                  WaitForCompletion blocks until snapshot completes
                  If false, snapshot is created asynchronously
                type: boolean
            required:
            - clusterRef
            - repository
            type: object
          status:
            description: OpenSearchSnapshotStatus defines the observed state of OpenSearchSnapshot
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              duration:
                description: Duration is the snapshot duration (human-readable)
                type: string
              endTime:
                description: EndTime is when the snapshot completed
                format: date-time
                type: string
              indices:
                description: Indices are the indices included in the snapshot
                items:
                  type: string
                type: array
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              phase:
                description: Phase is the current phase (Pending, InProgress, Completed,
                  Failed, Partial)
                type: string
              shards:
                description: Shards contains shard statistics
                properties:
                  failed:
                    description: Failed shards
                    format: int32
                    type: integer
                  successful:
                    description: Successful shards
                    format: int32
                    type: integer
                  total:
                    description: Total number of shards
                    format: int32
                    type: integer
                required:
                - failed
                - successful
                - total
                type: object
              snapshotName:
                description: |-
                  SnapshotName is the generated snapshot name in OpenSearch
                  Format: {crd-name}-{yyyyMMdd}-{HHmmss}
                type: string
              startTime:
                description: StartTime is when the snapshot started
                format: date-time
                type: string
              state:
                description: State is the OpenSearch snapshot state (IN_PROGRESS,
                  SUCCESS, FAILED, PARTIAL)
                type: string
              totalSize:
                description: TotalSize is the total snapshot size (human-readable)
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
