---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: opensearchroles.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchRole
    listKind: OpenSearchRoleList
    plural: opensearchroles
    shortNames:
    - osrole
    singular: opensearchrole
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.driftDetected
      name: Drift
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchRole is the Schema for the opensearchroles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchRoleSpec defines the desired state of OpenSearchRole
            properties:
              clusterPermissions:
                description: ClusterPermissions are cluster-level permissions
                items:
                  type: string
                type: array
              clusterRef:
                description: ClusterRef references the WazuhCluster this resource
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              description:
                description: Description is a human-readable description of the role
                maxLength: 1024
                type: string
              indexPermissions:
                description: IndexPermissions define index-level permissions
                items:
                  description: IndexPermission defines permissions for indices
                  properties:
                    allowedActions:
                      description: AllowedActions are the permitted actions
                      items:
                        type: string
                      minItems: 1
                      type: array
                    dls:
                      description: DLS is the document-level security query
                      type: string
                    fls:
                      description: FLS is the field-level security configuration
                      items:
                        type: string
                      type: array
                    indexPatterns:
                      description: IndexPatterns are index name patterns (e.g., "logs-*")
                      items:
                        type: string
                      minItems: 1
                      type: array
                    maskedFields:
                      description: MaskedFields are fields to be masked
                      items:
                        type: string
                      type: array
                  required:
                  - allowedActions
                  - indexPatterns
                  type: object
                type: array
              tenantPermissions:
                description: TenantPermissions define tenant-level permissions
                items:
                  description: TenantPermission defines permissions for tenants
                  properties:
                    allowedActions:
                      description: AllowedActions are the permitted actions (e.g.,
                        "kibana_all_read", "kibana_all_write")
                      items:
                        type: string
                      minItems: 1
                      type: array
                    tenantPatterns:
                      description: TenantPatterns are tenant name patterns
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - allowedActions
                  - tenantPatterns
                  type: object
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: OpenSearchRoleStatus defines the observed state of OpenSearchRole
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase is the current phase of the role (Pending, Ready,
                  Failed, Conflict)
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.driftDetected
      name: Drift
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenSearchRole is the Schema for the opensearchroles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchRoleSpec defines the desired state of OpenSearchRole
            properties:
              clusterPermissions:
                description: ClusterPermissions are cluster-level permissions
                items:
                  type: string
                type: array
              clusterRef:
                description: ClusterRef references the WazuhCluster this resource
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              description:
                description: Description is a human-readable description of the role
                type: string
              indexPermissions:
                description: IndexPermissions define index-level permissions
                items:
                  description: IndexPermission defines permissions for indices
                  properties:
                    allowedActions:
                      description: AllowedActions are the permitted actions
                      items:
                        type: string
                      type: array
                    dls:
                      description: DLS is the document-level security query
                      type: string
                    fls:
                      description: FLS is the field-level security configuration
                      items:
                        type: string
                      type: array
                    indexPatterns:
                      description: IndexPatterns are index name patterns (e.g., "logs-*")
                      items:
                        type: string
                      type: array
                    maskedFields:
                      description: MaskedFields are fields to be masked
                      items:
                        type: string
                      type: array
                  required:
                  - allowedActions
                  - indexPatterns
                  type: object
                type: array
              tenantPermissions:
                description: TenantPermissions define tenant-level permissions
                items:
                  description: TenantPermission defines permissions for tenants
                  properties:
                    allowedActions:
                      description: AllowedActions are the permitted actions (e.g.,
                        "kibana_all_read", "kibana_all_write")
                      items:
                        type: string
                      type: array
                    tenantPatterns:
                      description: TenantPatterns are tenant name patterns
                      items:
                        type: string
                      type: array
                  required:
                  - allowedActions
                  - tenantPatterns
                  type: object
                type: array
            required:
            - clusterRef
            type: object
          status:
            description: OpenSearchRoleStatus defines the observed state of OpenSearchRole
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase is the current phase of the role (Pending, Ready,
                  Failed, Conflict)
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
