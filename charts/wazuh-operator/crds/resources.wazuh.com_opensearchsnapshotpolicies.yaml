---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: opensearchsnapshotpolicies.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchSnapshotPolicy
    listKind: OpenSearchSnapshotPolicyList
    plural: opensearchsnapshotpolicies
    shortNames:
    - ossnap
    singular: opensearchsnapshotpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.repository.name
      name: Repository
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.snapshotCount
      name: Snapshots
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchSnapshotPolicy is the Schema for the opensearchsnapshotpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchSnapshotPolicySpec defines the desired state of
              OpenSearchSnapshotPolicy
            properties:
              clusterRef:
                description: ClusterRef references the WazuhCluster this resource
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              creation:
                description: Creation defines the snapshot creation schedule
                properties:
                  schedule:
                    description: Schedule defines when to create snapshots
                    properties:
                      expression:
                        description: Expression is the cron expression (e.g., "0 0
                          * * *")
                        type: string
                      timezone:
                        default: UTC
                        description: Timezone is the timezone (e.g., "UTC", "America/New_York")
                        type: string
                    required:
                    - expression
                    type: object
                  timeLimit:
                    description: TimeLimit is the maximum time allowed for snapshot
                      creation
                    type: string
                required:
                - schedule
                type: object
              deletion:
                description: Deletion defines the snapshot deletion policy
                properties:
                  condition:
                    description: Condition defines when to delete snapshots
                    properties:
                      maxAge:
                        description: MaxAge is the maximum age of snapshots to retain
                          (e.g., "30d")
                        type: string
                      maxCount:
                        description: MaxCount is the maximum number of snapshots to
                          retain
                        format: int32
                        type: integer
                      minCount:
                        description: MinCount is the minimum number of snapshots to
                          keep
                        format: int32
                        type: integer
                    type: object
                  schedule:
                    description: Schedule defines when to run deletion
                    properties:
                      expression:
                        description: Expression is the cron expression (e.g., "0 0
                          * * *")
                        type: string
                      timezone:
                        default: UTC
                        description: Timezone is the timezone (e.g., "UTC", "America/New_York")
                        type: string
                    required:
                    - expression
                    type: object
                type: object
              description:
                description: Description is a human-readable description
                type: string
              notification:
                description: Notification configures alerts for snapshot events
                properties:
                  channel:
                    description: Channel is the notification channel configuration
                    properties:
                      id:
                        description: ID is the notification channel ID
                        type: string
                    required:
                    - id
                    type: object
                  conditions:
                    description: Conditions define when to send notifications
                    properties:
                      creation:
                        description: Creation triggers notification on snapshot creation
                        type: boolean
                      deletion:
                        description: Deletion triggers notification on snapshot deletion
                        type: boolean
                      failure:
                        description: Failure triggers notification on failures
                        type: boolean
                    type: object
                type: object
              repository:
                description: Repository configures the snapshot repository
                properties:
                  name:
                    description: Name is the repository name
                    type: string
                  settings:
                    description: Settings are repository-specific settings
                    properties:
                      basePath:
                        description: BasePath is the path within the bucket
                        type: string
                      bucket:
                        description: Bucket is the S3/GCS/Azure bucket name
                        type: string
                      compress:
                        description: Compress enables compression
                        type: boolean
                      credentialsSecretRef:
                        description: CredentialsSecretRef references a Secret with
                          repository credentials
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the secret name
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint is a custom S3 endpoint
                        type: string
                      location:
                        description: Location is the filesystem path (for fs type)
                        type: string
                      region:
                        description: Region is the AWS region (for s3 type)
                        type: string
                    type: object
                  type:
                    description: Type is the repository type (fs, s3, azure, gcs)
                    enum:
                    - fs
                    - s3
                    - azure
                    - gcs
                    type: string
                required:
                - name
                - type
                type: object
              snapshotConfig:
                description: SnapshotConfig defines what to snapshot
                properties:
                  ignoreUnavailable:
                    description: IgnoreUnavailable skips unavailable indices
                    type: boolean
                  includeGlobalState:
                    description: IncludeGlobalState includes cluster state
                    type: boolean
                  indices:
                    description: Indices are index patterns to include
                    items:
                      type: string
                    type: array
                  partial:
                    description: Partial allows partial snapshots
                    type: boolean
                type: object
            required:
            - clusterRef
            - creation
            - repository
            type: object
          status:
            description: OpenSearchSnapshotPolicyStatus defines the observed state
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSnapshotTime:
                description: LastSnapshotTime is when the last snapshot was taken
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase is the current phase (Pending, Ready, Failed, Conflict)
                type: string
              repositoryStatus:
                description: RepositoryStatus is the status of the snapshot repository
                type: string
              snapshotCount:
                description: SnapshotCount is the number of snapshots
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.repository.name
      name: Repository
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.snapshotCount
      name: Snapshots
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenSearchSnapshotPolicy is the Schema for the opensearchsnapshotpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchSnapshotPolicySpec defines the desired state of
              OpenSearchSnapshotPolicy
            properties:
              clusterRef:
                description: ClusterRef references the WazuhCluster this resource
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              creation:
                description: Creation defines the snapshot creation schedule
                properties:
                  schedule:
                    description: Schedule defines when to create snapshots
                    properties:
                      expression:
                        description: Expression is the cron expression (e.g., "0 0
                          * * *")
                        type: string
                      timezone:
                        default: UTC
                        description: Timezone is the timezone (e.g., "UTC", "America/New_York")
                        type: string
                    required:
                    - expression
                    type: object
                  timeLimit:
                    description: TimeLimit is the maximum time allowed for snapshot
                      creation
                    type: string
                required:
                - schedule
                type: object
              deletion:
                description: Deletion defines the snapshot deletion policy
                properties:
                  condition:
                    description: Condition defines when to delete snapshots
                    properties:
                      maxAge:
                        description: MaxAge is the maximum age of snapshots to retain
                          (e.g., "30d")
                        type: string
                      maxCount:
                        description: MaxCount is the maximum number of snapshots to
                          retain
                        format: int32
                        type: integer
                      minCount:
                        description: MinCount is the minimum number of snapshots to
                          keep
                        format: int32
                        type: integer
                    type: object
                  schedule:
                    description: Schedule defines when to run deletion
                    properties:
                      expression:
                        description: Expression is the cron expression (e.g., "0 0
                          * * *")
                        type: string
                      timezone:
                        default: UTC
                        description: Timezone is the timezone (e.g., "UTC", "America/New_York")
                        type: string
                    required:
                    - expression
                    type: object
                type: object
              description:
                description: Description is a human-readable description
                type: string
              notification:
                description: Notification configures alerts for snapshot events
                properties:
                  channel:
                    description: Channel is the notification channel configuration
                    properties:
                      id:
                        description: ID is the notification channel ID
                        type: string
                    required:
                    - id
                    type: object
                  conditions:
                    description: Conditions define when to send notifications
                    properties:
                      creation:
                        description: Creation triggers notification on snapshot creation
                        type: boolean
                      deletion:
                        description: Deletion triggers notification on snapshot deletion
                        type: boolean
                      failure:
                        description: Failure triggers notification on failures
                        type: boolean
                    type: object
                type: object
              repository:
                description: Repository configures the snapshot repository
                properties:
                  name:
                    description: Name is the repository name
                    type: string
                  settings:
                    description: Settings are repository-specific settings
                    properties:
                      basePath:
                        description: BasePath is the path within the bucket
                        type: string
                      bucket:
                        description: Bucket is the S3/GCS/Azure bucket name
                        type: string
                      compress:
                        description: Compress enables compression
                        type: boolean
                      credentialsSecretRef:
                        description: CredentialsSecretRef references a Secret with
                          repository credentials
                        properties:
                          key:
                            default: password
                            description: Key is the key in the secret
                            type: string
                          name:
                            description: Name is the secret name
                            type: string
                        required:
                        - name
                        type: object
                      endpoint:
                        description: Endpoint is a custom S3 endpoint
                        type: string
                      location:
                        description: Location is the filesystem path (for fs type)
                        type: string
                      region:
                        description: Region is the AWS region (for s3 type)
                        type: string
                    type: object
                  type:
                    description: Type is the repository type (fs, s3, azure, gcs)
                    enum:
                    - fs
                    - s3
                    - azure
                    - gcs
                    type: string
                required:
                - name
                - type
                type: object
              snapshotConfig:
                description: SnapshotConfig defines what to snapshot
                properties:
                  ignoreUnavailable:
                    description: IgnoreUnavailable skips unavailable indices
                    type: boolean
                  includeGlobalState:
                    description: IncludeGlobalState includes cluster state
                    type: boolean
                  indices:
                    description: Indices are index patterns to include
                    items:
                      type: string
                    type: array
                  partial:
                    description: Partial allows partial snapshots
                    type: boolean
                type: object
            required:
            - clusterRef
            - creation
            - repository
            type: object
          status:
            description: OpenSearchSnapshotPolicyStatus defines the observed state
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSnapshotTime:
                description: LastSnapshotTime is when the last snapshot was taken
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase is the current phase (Pending, Ready, Failed, Conflict)
                type: string
              repositoryStatus:
                description: RepositoryStatus is the status of the snapshot repository
                type: string
              snapshotCount:
                description: SnapshotCount is the number of snapshots
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
