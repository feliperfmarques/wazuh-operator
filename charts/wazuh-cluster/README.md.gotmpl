{{ template "chart.header" . }}

## Prerequisites

- Kubernetes 1.25+
- Helm 3
- Wazuh Operator installed (use the `wazuh-operator` chart)

## Documentation

| Resource                                                       | Description                     |
| -------------------------------------------------------------- | ------------------------------- |
| [User Documentation](../../docs/usage/README.md)               | Full usage guide                |
| [Quick Start Examples](../../docs/usage/examples/quick-start/) | Minimal deployment examples     |
| [Production Examples](../../docs/usage/examples/production/)   | Production-ready configurations |
| [Sizing Profiles](../../docs/usage/features/sizing.md)         | Cluster sizing guide            |
| [CRD Reference](../../docs/usage/CRD-REFERENCE.md)             | Complete API documentation      |

## Installation

### Quick Start

1. Install the Wazuh Operator first (if not already installed):

```bash
helm template wazuh-operator ./charts/wazuh-operator \
  --namespace wazuh-operator | kubectl apply --server-side -f -
```

2. Install the chart with a sizing profile:

```bash
helm template my-wazuh-cluster ./charts/wazuh-cluster \
  --set sizing.profile=M \
  --namespace wazuh | kubectl apply --server-side -f -
```

## Upgrading

```bash
helm template my-wazuh-cluster ./charts/wazuh-cluster \
  -f my-values.yaml \
  --namespace wazuh | kubectl apply --server-side -f -
```

## Uninstallation

```bash
kubectl delete -f <(helm template my-wazuh-cluster ./charts/wazuh-cluster --namespace wazuh)
```

> **Note:** This will delete all WazuhCluster resources and secrets. The operator will clean up all associated Kubernetes resources.

## Values

{{ define "chart.valuesTableRow" }}| {{ .Key }} | {{ .Type }} | {{ if .Default }}{{ .Default }}{{ else }}{{ .AutoDefault }}{{ end }} | {{ if .Description }}{{ .Description }}{{ else }}{{ .AutoDescription }}{{ end }} |{{ end }}

### Global Parameters

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if or (eq .Key "nameOverride") (eq .Key "namespace") (eq .Key "createNamespace") }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Sizing Profiles

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "sizing" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Inline Secrets

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "secrets" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### External Secrets Operator (ESO)

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "externalSecrets" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Cluster General

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if or (eq .Key "cluster.enabled") (eq .Key "cluster.name") (eq .Key "cluster.spec.version") (eq .Key "cluster.spec.imagePullSecrets") }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Indexer Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.indexer" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Manager Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if and (hasPrefix "cluster.spec.manager" .Key) (not (hasPrefix "cluster.spec.manager.master" .Key)) (not (hasPrefix "cluster.spec.manager.workers" .Key)) }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Manager Master Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.manager.master" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Manager Workers Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.manager.workers" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Dashboard Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.dashboard" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### TLS Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.tls" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Monitoring Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.monitoring" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Drain Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "cluster.spec.drain" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchSnapshotRepository

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchRepository" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchSnapshotPolicy

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchSnapshotPolicy" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### WazuhBackup

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "wazuhBackup" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Backup Credentials

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "backupCredentials" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchUser

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchUsers" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchRole

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if eq "opensearchRoles" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchRoleMapping

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchRoleMappings" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchTenant

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchTenants" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchActionGroup

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchActionGroups" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchAuthConfig

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchAuthConfig" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchIndex

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if eq "opensearchIndices" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchIndexTemplate

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchIndexTemplates" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchComponentTemplate

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchComponentTemplates" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchISMPolicy

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchISMPolicies" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchSnapshot

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchSnapshots" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### OpenSearchRestore

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "opensearchRestores" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### WazuhRule

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "wazuhRules" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### WazuhDecoder

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "wazuhDecoders" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### WazuhRestore

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "wazuhRestore" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Network Policy Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "networkPolicy" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

### Resource Quota Configuration

| Key | Type | Default | Description |
|-----|------|---------|-------------|
{{- range .Values }}
{{- if hasPrefix "resourceQuota" .Key }}
{{ template "chart.valuesTableRow" . }}
{{- end }}
{{- end }}

## Examples

### Minimal Deployment

```yaml
sizing:
  profile: S

secrets:
  wazuhApi:
    password: "MySecurePassword123!"
  indexerAdmin:
    password: "MySecurePassword456!"
  wazuhAuthd:
    password: "MySecurePassword789!"
```

### Production with External Secrets (Vault)

```yaml
sizing:
  profile: L

# Disable inline secrets
secrets:
  wazuhApi:
    password: ""
  indexerAdmin:
    password: ""

# Use External Secrets Operator
externalSecrets:
  enabled: true
  indexerAdmin:
    name: wazuh-indexer-credentials
    secretStoreRef:
      name: vault-backend
      kind: SecretStore
    remoteRef:
      key: secret/data/wazuh/indexer
  wazuhApi:
    name: wazuh-api-credentials
    secretStoreRef:
      name: vault-backend
      kind: SecretStore
    remoteRef:
      key: secret/data/wazuh/api
```

### HPA and High Availability

```yaml
sizing:
  profile: L

cluster:
  spec:
    indexer:
      antiAffinity:
        enabled: true
        type: required
        topologyKey: topology.kubernetes.io/zone
    manager:
      workers:
        hpa:
          enabled: true
          minReplicas: 2
          maxReplicas: 10
          targetCPUUtilizationPercentage: 80
    dashboard:
      hpa:
        enabled: true
        minReplicas: 2
        maxReplicas: 5
```

### Gateway API Configuration

```yaml
cluster:
  spec:
    dashboard:
      gatewayAPI:
        enabled: true
        gatewayRef:
          name: wazuh-gateway
          namespace: gateway-system
        hostnames:
          - dashboard.wazuh.example.com
        http:
          pathPrefix: /
    manager:
      workers:
        gatewayAPI:
          enabled: true
          gatewayRef:
            name: wazuh-gateway
          tcp:
            enabled: true
            enrollmentEnabled: true
            eventsEnabled: true
```

### Ingress Configuration

```yaml
cluster:
  spec:
    dashboard:
      ingress:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: dashboard.wazuh.example.com
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: dashboard-tls
            hosts:
              - dashboard.wazuh.example.com
    indexer:
      ingress:
        enabled: true
        ingressClassName: nginx
        annotations:
          nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
        hosts:
          - host: indexer.wazuh.example.com
            paths:
              - path: /
                pathType: Prefix
    manager:
      master:
        ingress:
          enabled: true
          ingressClassName: nginx
          hosts:
            - host: manager.wazuh.example.com
              paths:
                - path: /
                  pathType: Prefix
      workers:
        ingress:
          enabled: true
          ingressClassName: nginx
          hosts:
            - host: manager-workers.wazuh.example.com
              paths:
                - path: /
                  pathType: Prefix
```

> **Note:** Ingress and Gateway API cannot both be enabled for the same component.

## Security Considerations

**IMPORTANT**: The default passwords in this chart are for demonstration purposes only.

For production use, you **MUST**:

1. Change all default passwords
2. Use External Secrets Operator for credential management
3. Enable TLS for all communications
4. Review and apply appropriate RBAC policies
5. Enable anti-affinity for high availability
6. Configure network policies

## Support

- GitHub Issues: <https://github.com/MaximeWewer/wazuh-operator/issues>
- Documentation: <https://documentation.wazuh.com/>
