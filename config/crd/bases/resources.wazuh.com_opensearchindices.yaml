---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: opensearchindices.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchIndex
    listKind: OpenSearchIndexList
    plural: opensearchindices
    shortNames:
    - osidx
    singular: opensearchindex
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .status.docsCount
      name: Docs
      type: integer
    - jsonPath: .status.storageSize
      name: Size
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchIndex is the Schema for the opensearchindices API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchIndexSpec defines the desired state of OpenSearchIndex
            properties:
              aliases:
                description: Aliases define index aliases
                items:
                  description: OpenSearchIndexAlias defines an alias for an OpenSearchIndex
                  properties:
                    filter:
                      description: Filter is an optional filter query for the alias
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    isWriteIndex:
                      description: IsWriteIndex marks this as the write index for
                        the alias
                      type: boolean
                    name:
                      description: Name is the alias name (must be lowercase, alphanumeric
                        with hyphens/underscores)
                      maxLength: 255
                      minLength: 1
                      pattern: ^[a-z0-9][a-z0-9_\-\.]*$
                      type: string
                    routing:
                      description: Routing is the routing value for the alias
                      type: string
                  required:
                  - name
                  type: object
                type: array
              clusterRef:
                description: ClusterRef references the WazuhCluster this resource
                  belongs to
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              mappings:
                description: Mappings define the index field mappings
                properties:
                  dateDetection:
                    description: DateDetection enables automatic date detection
                    type: boolean
                  dynamic:
                    description: Dynamic controls dynamic mapping behavior (true,
                      false, strict)
                    enum:
                    - true
                    - false
                    - strict
                    type: string
                  numericDetection:
                    description: NumericDetection enables automatic numeric detection
                    type: boolean
                  properties:
                    description: Properties define the field mappings
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              settings:
                description: Settings are the index settings
                properties:
                  codec:
                    description: Codec is the compression codec (default or best_compression)
                    enum:
                    - default
                    - best_compression
                    type: string
                  custom:
                    description: Custom contains additional settings as raw JSON
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  numberOfReplicas:
                    default: 1
                    description: NumberOfReplicas is the number of replica shards
                    format: int32
                    minimum: 0
                    type: integer
                  numberOfShards:
                    default: 1
                    description: NumberOfShards is the number of primary shards
                    format: int32
                    minimum: 1
                    type: integer
                  refreshInterval:
                    description: RefreshInterval is how often to refresh the index
                    type: string
                type: object
            required:
            - clusterRef
            type: object
          status:
            description: OpenSearchIndexStatus defines the observed state of OpenSearchIndex
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              docsCount:
                description: DocsCount is the number of documents in the index
                format: int64
                type: integer
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              health:
                description: Health is the index health (green, yellow, red)
                type: string
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase is the current phase (Pending, Ready, Failed, Conflict)
                enum:
                - Pending
                - Ready
                - Failed
                type: string
              primaryShards:
                description: PrimaryShards is the actual number of primary shards
                format: int32
                type: integer
              replicaShards:
                description: ReplicaShards is the actual number of replica shards
                format: int32
                type: integer
              storageSize:
                description: StorageSize is the storage size of the index
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
