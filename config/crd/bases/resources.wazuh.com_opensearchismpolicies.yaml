---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: opensearchismpolicies.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: OpenSearchISMPolicy
    listKind: OpenSearchISMPolicyList
    plural: opensearchismpolicies
    shortNames:
    - osism
    singular: opensearchismpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.defaultState
      name: Default State
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.driftDetected
      name: Drift
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: OpenSearchISMPolicy is the Schema for the opensearchismpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenSearchISMPolicySpec defines the desired state of OpenSearchISMPolicy
            properties:
              clusterRef:
                description: WazuhCluster reference
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              defaultState:
                description: Default state for the policy
                type: string
              description:
                description: Description of the ISM policy
                type: string
              ismTemplate:
                description: ISM template for automatic policy assignment
                items:
                  description: ISMTemplateConfig defines automatic policy assignment
                    configuration
                  properties:
                    indexPatterns:
                      description: Index patterns for automatic assignment
                      items:
                        type: string
                      minItems: 1
                      type: array
                    priority:
                      description: Priority for template matching
                      format: int32
                      type: integer
                  required:
                  - indexPatterns
                  type: object
                type: array
              states:
                description: States in the ISM policy
                items:
                  description: ISMState defines a state in the ISM policy
                  properties:
                    actions:
                      description: Actions to perform in this state
                      items:
                        description: ISMAction defines an action in an ISM state
                        properties:
                          config:
                            description: Action configuration (raw JSON)
                            type: object
                            x-kubernetes-preserve-unknown-fields: true
                          retry:
                            description: Retry configuration
                            properties:
                              backoff:
                                default: exponential
                                description: Backoff strategy
                                enum:
                                - exponential
                                - linear
                                - constant
                                type: string
                              count:
                                default: 3
                                description: Number of retries
                                format: int32
                                type: integer
                              delay:
                                description: Delay between retries
                                type: string
                            type: object
                          timeout:
                            description: Timeout for the action
                            type: string
                        required:
                        - config
                        type: object
                      type: array
                    name:
                      description: Name of the state
                      type: string
                    transitions:
                      description: Transitions to other states
                      items:
                        description: ISMTransition defines a transition to another
                          state
                        properties:
                          conditions:
                            description: Conditions for the transition
                            properties:
                              cron:
                                description: CRON expression for scheduled transition
                                properties:
                                  expression:
                                    description: CRON expression
                                    type: string
                                  timezone:
                                    default: UTC
                                    description: Timezone for CRON expression
                                    type: string
                                required:
                                - expression
                                type: object
                              minDocCount:
                                description: Minimum document count for transition
                                format: int64
                                type: integer
                              minIndexAge:
                                description: Minimum index age for transition
                                type: string
                              minSize:
                                description: Minimum size for transition
                                type: string
                            type: object
                          stateName:
                            description: State to transition to
                            type: string
                        required:
                        - stateName
                        type: object
                      type: array
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
            required:
            - clusterRef
            - defaultState
            - states
            type: object
          status:
            description: OpenSearchISMPolicyStatus defines the observed state of OpenSearchISMPolicy
            properties:
              affectedIndices:
                description: AffectedIndices is the number of indices using this policy
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of the ISM policy's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              conflictsWith:
                description: ConflictsWith is the namespace/name of a conflicting
                  CRD
                type: string
              driftDetected:
                description: DriftDetected indicates if manual modification was detected
                type: boolean
              lastAppliedHash:
                description: LastAppliedHash is the hash of the last applied spec
                  for drift detection
                type: string
              lastDriftTime:
                description: LastDriftTime is when drift was last detected
                format: date-time
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last synced to
                  OpenSearch
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation
                format: int64
                type: integer
              ownershipClaimed:
                description: OwnershipClaimed indicates if this CRD owns the OpenSearch
                  resource
                type: boolean
              phase:
                description: Phase represents the current phase of the ISM policy
                enum:
                - Pending
                - Ready
                - Failed
                type: string
              policyID:
                description: PolicyID in OpenSearch
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
