---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: wazuhcertificates.resources.wazuh.com
spec:
  group: resources.wazuh.com
  names:
    kind: WazuhCertificate
    listKind: WazuhCertificateList
    plural: wazuhcertificates
    shortNames:
    - wzcert
    singular: wazuhcertificate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.expiresAt
      name: Expires
      type: date
    - jsonPath: .status.daysUntilExpiry
      name: Days Left
      type: integer
    - jsonPath: .spec.autoRenewal.enabled
      name: Auto-Renew
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: WazuhCertificate is the Schema for the wazuhcertificates API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WazuhCertificateSpec defines the desired state of WazuhCertificate
            properties:
              autoGenerateSANs:
                description: |-
                  Auto-generate SANs based on certificate type and cluster topology
                  When enabled, SANs are automatically generated based on the type:
                  - indexer: *.indexer-nodes.svc, all indexer pod names
                  - admin: localhost only (admin cert)
                  - node: general node SANs (same as indexer)
                  - filebeat: manager master/worker service names
                  - dashboard: dashboard service names
                properties:
                  additionalSANs:
                    description: Additional custom SANs to include alongside auto-generated
                      ones
                    items:
                      type: string
                    type: array
                  enabled:
                    default: true
                    description: Enable auto-generation of SANs based on certificate
                      type
                    type: boolean
                  includeIPSANs:
                    default: false
                    description: Include IP SANs (e.g., 127.0.0.1)
                    type: boolean
                  indexerReplicas:
                    default: 3
                    description: Number of indexer replicas (used to generate pod-specific
                      SANs)
                    format: int32
                    type: integer
                  namespace:
                    description: Namespace for generating fully-qualified DNS names
                    type: string
                type: object
              autoRenewal:
                description: Auto-renewal configuration
                properties:
                  enabled:
                    default: true
                    description: Enable automatic renewal
                    type: boolean
                  failedJobsHistoryLimit:
                    default: 1
                    description: Number of failed job history to keep
                    format: int32
                    type: integer
                  schedule:
                    default: 0 2 * * *
                    description: 'CronJob schedule for renewal checks (default: daily
                      at 2 AM)'
                    type: string
                  successfulJobsHistoryLimit:
                    default: 3
                    description: Number of successful job history to keep
                    format: int32
                    type: integer
                  thresholdDays:
                    default: 30
                    description: Renew certificate X days before expiry
                    maximum: 365
                    minimum: 1
                    type: integer
                type: object
              clusterRef:
                description: Target cluster for certificate management
                properties:
                  name:
                    description: Name of the WazuhCluster resource
                    type: string
                  namespace:
                    description: |-
                      Namespace of the WazuhCluster resource
                      If empty, assumes the same namespace as the referencing resource
                    type: string
                required:
                - name
                type: object
              distinguishedName:
                description: Distinguished Name configuration
                properties:
                  commonName:
                    description: Common Name (CN)
                    type: string
                  country:
                    default: US
                    description: Country (C) - 2 letter code
                    pattern: ^[A-Z]{2}$
                    type: string
                  emailAddress:
                    description: Email Address
                    type: string
                  locality:
                    default: San Francisco
                    description: Locality/City (L)
                    type: string
                  organization:
                    default: Wazuh
                    description: Organization (O)
                    type: string
                  organizationalUnit:
                    default: Security
                    description: Organizational Unit (OU)
                    type: string
                  state:
                    default: California
                    description: State/Province (ST)
                    type: string
                required:
                - commonName
                type: object
              hotReload:
                default: false
                description: Enable hot-reload for OpenSearch (requires 2.13+)
                type: boolean
              keyConfig:
                description: Key algorithm and size
                properties:
                  algorithm:
                    default: RSA
                    description: Key algorithm (RSA, ECDSA)
                    enum:
                    - RSA
                    - ECDSA
                    type: string
                  curve:
                    default: P256
                    description: ECDSA curve (P256, P384, P521)
                    enum:
                    - P256
                    - P384
                    - P521
                    type: string
                  size:
                    default: 2048
                    description: Key size for RSA (2048, 3072, 4096)
                    enum:
                    - 2048
                    - 3072
                    - 4096
                    type: integer
                type: object
              sans:
                description: Subject Alternative Names (explicit list)
                items:
                  type: string
                type: array
              secretName:
                description: Secret name to store the certificate
                type: string
              type:
                allOf:
                - enum:
                  - ca
                  - node
                  - admin
                  - filebeat
                  - indexer
                  - dashboard
                - enum:
                  - ca
                  - node
                  - admin
                  - filebeat
                  - indexer
                  - dashboard
                description: Certificate type (ca, node, admin, filebeat)
                type: string
              validity:
                default: 365d
                description: |-
                  Certificate validity as a duration string
                  Format: "<value><unit>" where unit is d (days), h (hours), or m (minutes)
                  Examples: "365d" (1 year), "24h" (1 day), "30m" (30 minutes)
                pattern: ^\d+[dhm]$
                type: string
            required:
            - clusterRef
            - secretName
            - type
            type: object
          status:
            description: WazuhCertificateStatus defines the observed state of WazuhCertificate
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              daysUntilExpiry:
                description: Days until expiry
                type: integer
              expiresAt:
                description: Certificate expiry timestamp
                format: date-time
                type: string
              issuedAt:
                description: Certificate issued at timestamp
                format: date-time
                type: string
              lastRenewalAttempt:
                description: Last renewal attempt
                format: date-time
                type: string
              observedGeneration:
                description: Observed generation
                format: int64
                type: integer
              phase:
                description: Phase of certificate (Pending, Ready, Renewing, Failed)
                enum:
                - Pending
                - Ready
                - Renewing
                - Failed
                - Expired
                type: string
              renewalStatus:
                description: Renewal status
                properties:
                  jobName:
                    description: Renewal job name
                    type: string
                  lastRenewed:
                    description: Last renewal time
                    format: date-time
                    type: string
                  needed:
                    description: Renewal needed
                    type: boolean
                  nextCheck:
                    description: Next scheduled renewal check
                    format: date-time
                    type: string
                type: object
              secretRef:
                description: Secret reference where cert is stored
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              serialNumber:
                description: Certificate serial number
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
